import type { CollectionLiveSurfaceSnapshot } from './collection-live-surface-data.ts';

export async function syncCollectionLiveSurface(_snapshot?: CollectionLiveSurfaceSnapshot) {
  // Web and generic bundles have no operating-system Live Activity surface.
}
